# list of unsupervised losses: "pca" | "temporal", default: null. if null, assume a model is supervised.
losses_to_use: null # ["temporal"]

# resnet version for backbone: 18 | 34 | 50 | 101 | 152
resnet_version: 50

# prediction mode: "heatmap" | "regression"
#model_type: "heatmap"
model_type: "regression"

# directory name for model saving
model_name: "resnet50"
